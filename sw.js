self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch{};event.waitUntil(self.registration.showNotification(data.title||'📖 Reading check-in!',{body:data.body||'What page are you on? 💕',data:{url:data.url||'./',bookId:data.bookId||null},tag:'reading-check-in',renotify:false}))});self.addEventListener('notificationclick',event=>{event.notification.close();const target=new URL(event.notification.data?.url||'./',self.location.origin+self.registration.scope).href;event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus'in c){c.navigate(target);return c.focus()}}return clients.openWindow?clients.openWindow(target):null}))});